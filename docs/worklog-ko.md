# 작업 로그 (KO)

아래 표는 날짜/주차 기준으로 주요 변경사항을 관리하기 위한 템플릿이며,
현재 저장소에서 확인 가능한 마이그레이션 이력과 이슈를 정식 항목으로 정리했습니다.

| 날짜/주차 | 작업 내용 | 이슈 | 해결 | 영향 |
| --- | --- | --- | --- | --- |
| 2026-W01 | React 엔트리(`src/main.tsx`)와 앱 셸(`src/App.tsx`) 기준으로 UI/엔진 경계 재구성 | 기존 단일 흐름 구조에서 UI-엔진 결합도가 높아 변경 영향 범위가 넓음 | `GameEngine` 인스턴스를 React 수명주기에 연결하고, UI 이벤트를 엔진 API로 전달 | 상태/이벤트 흐름이 명확해져 기능 추가 시 회귀 위험 감소 |
| 2026-W01 | 엔진 모듈 TS 전환 및 타입 계약 정리(`src/engine/GameEngine.ts`, `src/types/game.ts`) | 모듈 간 타입 기준이 분산되어 협업 시 해석 불일치 발생 가능 | 공용 타입 파일에 핵심 도메인 타입을 모아 입력/엔진/플레이어 계층에서 공유 | 리팩터링 안정성 향상, 코드 리뷰 시 계약 검증 용이 |
| 2026-W02 | 입력 처리 안정화 및 재시작 동작 점검 | **R키 잠금 이슈**: 이동 중/클리어 상태 잠금(`isLocked`) 조건 때문에 재시작 입력이 무시될 수 있음 | `R` 키 이벤트를 잠금 검사보다 먼저 처리하도록 입력 로직 조정 | 긴급 복구(재시작) UX 보장, 테스트/디버깅 반복 속도 개선 |
| 2026-W02 | 마이그레이션 완료 기준 문서화(README, migration-plan) | strict 적용 상태 및 후속 작업 가시성이 낮음 | 체크리스트에 strict 미적용/후속 과제를 명시 | 팀 단위 일정/우선순위 합의가 쉬워짐 |

---

## 재사용 템플릿 (빈 양식)

| 날짜/주차 | 작업 내용 | 이슈 | 해결 | 영향 |
| --- | --- | --- | --- | --- |
| YYYY-W## |  |  |  |  |
| YYYY-W## |  |  |  |  |
